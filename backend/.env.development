# Database Configuration (locale)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=brainarena

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (per sviluppo locale)
ALLOWED_ORIGINS=http://localhost:4000,https://localhost:4000,http://localhost:5173